## 7. DEPLOYMENT INSTRUCTIONS
```bash
# deployment_guide.sh

# === LOCAL DEVELOPMENT ===
# 1. Clone repository
git clone <your-repo-url>
cd omnibiomol

# 2. Create virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# 3. Install dependencies
pip install -r requirements.txt

# 4. Run locally
streamlit run app.py

# === STREAMLIT CLOUD DEPLOYMENT ===
# 1. Push code to GitHub
git add .
git commit -m "Initial OmniBiMol Phase 1 MVP"
git push origin main

# 2. Go to https://share.streamlit.io
# 3. Connect your GitHub repository
# 4. Select main branch and app.py
# 5. Deploy!

# === HEROKU DEPLOYMENT (Alternative) ===
# 1. Create Procfile
echo "web: sh setup.sh && streamlit run app.py" > Procfile

# 2. Create setup.sh
cat > setup.sh << 'EOF'
mkdir -p ~/.streamlit/
echo "\
[general]\n\
email = \"your-email@domain.com\"\n\
" > ~/.streamlit/credentials.toml
echo "\
[server]\n\
headless = true\n\
enableCORS=false\n\
port = $PORT\n\
" > ~/.streamlit/config.toml
EOF

# 3. Create runtime.txt
echo "python-3.11.7" > runtime.txt

# 4. Deploy to Heroku
heroku create omnibiomol-app
git push heroku main
heroku open

# === DOCKER DEPLOYMENT ===
# Create Dockerfile
cat > Dockerfile << 'EOF'
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
EOF

# Build and run
docker build -t omnibiomol .
docker run -p 8501:8501 omnibiomol
```

## 8. TESTING CHECKLIST
```python
# test_suite.py - Automated testing checklist

"""
TESTING CHECKLIST FOR OMNIBIOMOL PHASE 1 MVP

Test Proteins with Expected Results:
-------------------------------------

1. TP53 (Tumor Protein P53)
   - UniProt ID: P04637
   - Expected: High expression in multiple tissues, nuclear localization
   - Function: Tumor suppressor, DNA damage response
   - GO Terms: >50 terms across all categories
   - Tissue Expression: Detected in 44 tissues
   - Test Query: "TP53" or "p53"

2. BRCA1 (Breast Cancer Type 1)
   - UniProt ID: P38398
   - Expected: DNA repair protein, nuclear localization
   - Function: DNA damage repair, cell cycle regulation
   - Tissue Expression: Ubiquitous low-medium expression
   - Test Query: "BRCA1"

3. INS (Insulin)
   - UniProt ID: P01308
   - Expected: High in pancreatic islets, secreted protein
   - Function: Hormone regulating glucose metabolism
   - Tissue Expression: Pancreas (high), liver (low)
   - Subcellular: Secreted, extracellular
   - Test Query: "Insulin" or "INS"

4. ALB (Albumin)
   - UniProt ID: P02768
   - Expected: Liver expression, secreted/plasma protein
   - Function: Serum protein, carrier protein
   - Tissue Expression: Liver (high)
   - Subcellular: Secreted
   - Test Query: "Albumin"

5. EGFR (Epidermal Growth Factor Receptor)
   - UniProt ID: P00533
   - Expected: Membrane protein, multiple tissues
   - Function: Tyrosine kinase receptor
   - Tissue Expression: Widespread
   - Subcellular: Cell membrane, plasma membrane
   - Test Query: "EGFR"

FUNCTIONAL TESTS:
-----------------

✓ Input Validation:
  [ ] Empty input shows error
  [ ] Invalid protein name shows "No results"
  [ ] Special characters handled correctly
  [ ] Case-insensitive search works

✓ UniProt Integration:
  [ ] Single match auto-loads
  [ ] Multiple matches show selection
  [ ] UniProt ID direct input works
  [ ] Gene symbol search works
  [ ] Protein name search works

✓ Data Fetching:
  [ ] Parallel API calls complete < 10 seconds
  [ ] Cache hit shows instant results
  [ ] Error handling for API failures
  [ ] Missing data shows appropriate warnings

✓ Visualizations:
  [ ] Tissue expression bar chart renders
  [ ] Top 20 tissues prioritized correctly
  [ ] Subcellular heatmap displays
  [ ] GO terms chart shows distribution
  [ ] Charts are responsive (mobile/desktop)
  [ ] Hover tooltips work

✓ Data Export:
  [ ] CSV downloads contain correct data
  [ ] Filenames include UniProt ID
  [ ] All three export buttons functional

✓ Performance:
  [ ] First load < 10 seconds
  [ ] Cached load < 1 second
  [ ] No memory leaks on repeated searches
  [ ] Mobile responsive

✓ Error Scenarios:
  [ ] Network timeout handled
  [ ] 404 from HPA shows warning
  [ ] Invalid UniProt ID handled
  [ ] Empty API response handled

MANUAL TESTING PROCEDURE:
-------------------------

1. Test each of the 5 proteins listed above
2. Verify data accuracy against UniProt.org and proteinatlas.org
3. Test on different browsers (Chrome, Firefox, Safari)
4. Test on mobile device
5. Clear cache and test fresh load
6. Test with cached data
7. Test error scenarios (disconnect network)
8. Verify all download buttons
9. Check console for errors
10. Load test with rapid successive searches

SUCCESS CRITERIA:
-----------------
✓ All 5 test proteins load successfully
✓ Response time < 10s (fresh) / < 1s (cached)
✓ All visualizations render correctly
✓ No JavaScript errors in console
✓ Mobile responsive layout works
✓ Data accuracy verified against source databases
✓ Export functionality works for all formats
✓ Error handling graceful (no crashes)
"""

# Quick test runner
def run_quick_tests():
    """Run automated tests"""
    test_proteins = ["TP53", "BRCA1", "INS", "ALB", "EGFR"]
    
    print("🧪 OmniBiMol Phase 1 Test Suite")
    print("=" * 50)
    
    for protein in test_proteins:
        print(f"\nTesting: {protein}")
        print("-" * 50)
        
        # Test would go here
        # This is a template - actual implementation would use pytest
        
    print("\n✅ All tests completed!")

if __name__ == "__main__":
    run_quick_tests()
```

---

## COMPLETE FILE STRUCTURE